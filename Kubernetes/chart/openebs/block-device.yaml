 apiVersion: openebs.io/v1alpha1
 kind: BlockDevice
 metadata:
   name: blockdevice-by-manual
   namespace: openebs
   labels:
     kubernetes.io/hostname: kube
     ndm.io/managed: "false"
     ndm.io/blockdevice-type: blockdevice
 status:
   claimState: Unclaimed
   state: Active
 spec:
   capacity:
     logicalSectorSize: 512
     storage: 1000204886016
   details:
   # 通过 hdparm -I /dev/sdb 查询硬盘固件信息。但是这些东西都可以不填
     firmwareRevision: SN03
     model: ST1000NM0011
     serial: Z1N47BLZ
     compliance:
     vendor:
   devlinks:
   # 可以通过 udevadm info -q property -n /dev/sdb 查询路径信息
   - kind: by-id
     links:
       - /dev/disk/by-id/ata-ST1000NM0011_Z1N47BLZ
       - /dev/disk/by-id/wwn-0x5000c50063ad26de
   - kind: by-path
     links:
     - /dev/disk/by-path/pci-0000:01:00.0-scsi-0:1:1:0
   nodeAttributes:
     nodeName: kube
   path: /dev/sdb